# ---------- Core API ----------
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.0
email-validator>=2.0
slowapi>=0.1.9

# ---------- Database ----------
SQLAlchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13
pgvector>=0.3

# ---------- Background jobs / queue ----------
redis>=5.0
rq>=1.15,<3.0
rq-scheduler>=0.14.0

# ---------- AI / LLM ----------
anthropic>=0.78
openai>=2.0
# sentence-transformers omitted — large (PyTorch); use voyage/openai embeddings in production

# ---------- Billing / Email ----------
stripe>=5.0
resend>=2.0

# ---------- Resume parsing ----------
python-docx>=1.1.0
pypdf>=4.0.0

# ---------- Env / config ----------
python-dotenv>=1.0

# ---------- Utilities ----------
click>=8.0
typing-extensions>=4.9
httpx>=0.27

# ---------- Security ----------
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3

# ---------- Observability ----------
sentry-sdk[fastapi]>=2.0
