# ---------- Core API ----------
fastapi>=0.110
uvicorn[standard]>=0.29

# ---------- Database ----------
SQLAlchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13

# ---------- Background jobs / queue ----------
redis>=5.0
rq>=1.15,<3.0

# ---------- Resume parsing ----------
python-docx>=1.1.0
pypdf>=4.0.0

# ---------- Env / config ----------
python-dotenv>=1.0

# ---------- Utilities ----------
click>=8.0
typing-extensions>=4.9

# ---------- Security (future auth work, safe to include now) ----------
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3

# ---------- Observability ----------
sentry-sdk[fastapi]>=2.0

# ---------- Optional but recommended ----------
httpx>=0.27
